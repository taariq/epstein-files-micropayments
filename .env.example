# SerenDB (Document Database)
SERENDB_CONNECTION_STRING=postgresql://user:password@host:5432/documents_db

# x402 Gateway (live at https://x402.serendb.com/)
X402_GATEWAY_URL=https://x402.serendb.com
X402_PROVIDER_ID=uuid-from-registration
X402_API_KEY=key-from-registration

# Provider wallet (your wallet for receiving payments)
PROVIDER_WALLET_ADDRESS=0x...

# Agent wallet private key (OPTIONAL - for automatic payment signing)
# WARNING: This gives full control over the agent wallet. Store securely!
# - Required for automatic EIP-3009 payment signing
# - Agent must fund this wallet with USDC on Base network
# - Format: 64-character hex string with 0x prefix
# - Never commit this to version control
# - If not set, queries will return PaymentRequirements for manual signing
# AGENT_PRIVATE_KEY=0x...
